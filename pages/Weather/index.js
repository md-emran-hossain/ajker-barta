import React from 'react';
import Weather from '../../components/Weather/Weather';

const index = () => {
    return (
        <div>
            <Weather/>
        </div>
    );
};

export default index;